[project]
name = "cc"
version = "0.1.0"
description = "learn-claude-code"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.96.0",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]
